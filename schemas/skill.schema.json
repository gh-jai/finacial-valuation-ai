{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fvi.example/schemas/skill.schema.json",
  "title": "FVI Skill Document Frontmatter",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "title", "type", "status", "version", "domain", "source_refs", "dependencies", "owner", "last_updated", "inputs", "outputs"],
  "properties": {
    "id": {"type": "string", "pattern": "^SKL-[A-Z]+-[0-9]{3}$"},
    "title": {"type": "string", "minLength": 1},
    "type": {"const": "skill"},
    "status": {"enum": ["draft", "reviewed", "approved", "deprecated"]},
    "version": {"type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"},
    "domain": {"enum": ["accounting", "valuation", "risk", "narrative", "lifecycle", "market-pricing", "special-situations", "cross-domain"]},
    "source_refs": {"$ref": "#/$defs/idList"},
    "dependencies": {"$ref": "#/$defs/idList"},
    "owner": {"type": "string", "minLength": 1},
    "last_updated": {"type": "string", "format": "date"},
    "inputs": {"type": "array", "items": {"type": "string", "minLength": 1}, "uniqueItems": true},
    "outputs": {"type": "array", "items": {"type": "string", "minLength": 1}, "uniqueItems": true}
  },
  "$defs": {
    "idList": {"type": "array", "items": {"type": "string", "minLength": 1}, "uniqueItems": true}
  }
}
